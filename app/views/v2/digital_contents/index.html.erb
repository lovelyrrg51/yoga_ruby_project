<section id="quotes-banner" class="margin-bottom-0">
  <div class="light_overlay"></div>
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-12 col-md-12">
        <div class="banner_text">
          <div class="main_banner_heading">
            Digital Content
          </div>
        </div>
      </div>
      <div class="clearfix"></div>
    </div>
  </div>
</section>

<div class="clearfix"></div>

<div class="clearfix"></div>

<section class="event-course">
  <div class="container">
    <div class="heading_title">Digital Content for <span>Cosmic Therapist</span></div>
    <div class="row">

    <% @videos.each do |video| %>
      <div class="col-md-4 clearfix">
        <div class="course_box hover_active hover_shadow">
          <div class="course_video">
            <div class="shivyog_video_img">
              <%= link_to v2_digital_content_path(video.id), remote: true do %>
                <%= image_tag video.image, class: "img-fluid img-no-border" %>
              <% end %>
            </div>
          </div>
          <div class="course_title"><strong><a href="Javascript: void(0);"><%= video.title %></a></strong></div>
        </div>
      </div>
    <% end %>

    </div>
  </div>
</section>

<%= render "v2/shared/newsletter" %>

<script type="text/javascript">
  function makeTimer() {
    //  var endTime = new Date("29 April 2018 9:56:00 GMT+01:00");
    var endTime = new Date("19 April 2019 9:56:00 GMT+01:00");
    endTime = (Date.parse(endTime) / 1000);

    var now = new Date();
    now = (Date.parse(now) / 1000);
    var timeLeft = endTime - now;
    var days = Math.floor(timeLeft / 86400);
    var hours = Math.floor((timeLeft - (days * 86400)) / 3600);
    var minutes = Math.floor((timeLeft - (days * 86400) - (hours * 3600 )) / 60);
    var seconds = Math.floor((timeLeft - (days * 86400) - (hours * 3600) - (minutes * 60)));

    if (hours < "10") { hours = "0" + hours; }
    if (minutes < "10") { minutes = "0" + minutes; }
    if (seconds < "10") { seconds = "0" + seconds; }

    $(".days").html(days + "<span>Days</span>");
    $(".hours").html(hours + "<span>Hours</span>");
    $(".minutes").html(minutes + "<span>Minutes</span>");
    $(".seconds").html(seconds + "<span>Seconds</span>");
  }
  setInterval(function() { makeTimer(); }, 1000);
</script>
