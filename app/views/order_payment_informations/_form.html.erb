<%= form_for(@order_payment_information) do |f| %>
  <% if @order_payment_information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_payment_information.errors.count, "error") %> prohibited this order_payment_information from being saved:</h2>

      <ul>
      <% @order_payment_information.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
