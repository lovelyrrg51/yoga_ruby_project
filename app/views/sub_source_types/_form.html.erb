<%= form_for [@source_info_type, @sub_source_type], html:{class: "table-screen-next", id: "sub_source_type_form"} do |f| %>
  <div class="prelative mg-b30">
    <div class="overlapping "></div>
    <div class="row mg-t30">
        <div class="col-sm-12">
            <div class="box bordered-input">
                <label class="asterisk">Sub Source Name</label>
                <div class="dropdown dropdown-field">
                    <%= f.text_field :sub_source_name, class: 'form-control noBorder field', required: true, placeholder: "Sub source name" %>
                </div>
            </div>
        </div>
        <div class="col-sm-12">
            <div class="pannel-btn text-right">
                <button type="reset" class="btn btn-danger mg-r10" >CLEAR</button>
                <%= f.submit params[:action] == "edit" ? "UPDATE" : 'CREATE', {class: 'btn btn-info'} %>
            </div>
        </div>
    </div>
  </div>
<% end %>
<script type="text/javascript">
  $(document).ready(function(){
    $("#sub_source_type_form").validate();
  });
</script>
