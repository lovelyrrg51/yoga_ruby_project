<tr>
  <td align="center" valign="top">
    <table width="650" id="main" border="0" cellpadding="0" cellspacing="20">
      <tr>
        <td class="divider" colspan="2" bgcolor="#555555" height="2"></td>
      </tr>
      <tr class="episode">
        <td>
          <a href="#"><img src="https://dmhtmt59mq15j.cloudfront.net/assets/images/pic3-2597e250cc000b0043c28b1fcbfe91cc.jpg" height="185px" alt=<%= "Getting Started with #{Rails.application.config.app_base_url}"%> ></a>
        </td>
        <% payment = @options[:payment] %>
        <% if @options[:is_error] %>
          <td valign="top">
            <h2>Namah Shivay <%= @options[:user].try(:sadhak_profile).try(:full_name) %></h2>
            <h3> Something went wrong with your bulk file upload. </h3>
            <h3>Error: <%= @options[:message] %></h3>
            <h4><a href=<%="http://#{Rails.application.config.app_base_url}/"%> style="color:#900;">Click here</a> to visit our website.</h4>
          </td>
        <% else %>
          <td valign="top">
            <h2>Namah Shivay <%= @options[:user].try(:sadhak_profile).try(:full_name) %></h2>
            <h4>We have successfully processed your bulk upload.</h4>
            <% if payment.present? %>
              <h4>Reference Number: <%= payment.try(:event_order).try(:reg_ref_number) %>, Amount: <%= ("%.2f" % payment.try(:amount).to_f) %></h4>
            <% end %>
            <h4>Please find attachments.</h4>
            <!-- <h4><a href=<%#="http://#{Rails.application.config.app_base_url}/#/help"%> style="color:#900;">Click here</a> to learn how to register.</h4> -->
            <h4><a href=<%="http://#{Rails.application.config.app_base_url}/"%> style="color:#900;">Click here</a> to learn how to register.</h4>
            <h4><a href=<%="http://#{Rails.application.config.app_base_url}/"%> style="color:#900;">Click here</a> to visit our website.</h4>
          </td>
        <% end %>
      </tr>
      <tr>
        <td class="divider" colspan="2" bgcolor="#555555" height="2"></td>
      </tr>
    </table>
  </td>
</tr>