require 'rails_helper'

RSpec.describe PgSyPaypalStandardPayment, type: :model do
  # table is not present

  # describe "associations" do
  #   it { should belong_to(:event_order)}
  #   it { should belong_to(:sy_club)}
  # end

  # it do
  #   should define_enum_for(:status).with_values({pending: 0, success: 1, failure: 2})
  # end

  # context 'for serialize' do
  #   it { should serialize(:notification_params) }
  #   it { should serialize(:custom_data) }
  # end

end
