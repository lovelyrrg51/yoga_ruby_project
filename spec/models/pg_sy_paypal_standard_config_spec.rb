require 'rails_helper'

RSpec.describe PgSyPaypalStandardConfig, type: :model do

  # table is not exist

  # describe "associations" do
  #   it { should belong_to(:payment_gateway)}
  #   it { should belong_to(:db_country).class_name('DbCountry').with_foreign_key('country_id')}
  # end

  # it { should delegate_method(:currency_code).to(:db_country).allow_nil }
end
