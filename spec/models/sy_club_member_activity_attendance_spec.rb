require 'rails_helper'

RSpec.describe SyClubMemberActivityAttendance, type: :model do

  # Table is not present

  # describe "associations" do
  #   it { should belong_to(:sadhak_profile) }
  #   it { should belong_to(:sy_club_member) }
  #   it { should belong_to(:event) }
  #   it { should belong_to(:sy_club) }
  # end

  # describe "validations" do
  #   it { should validate_presence_of(:sadhak_profile_id) }
  #   it { should validate_presence_of(:sy_club_member_id) }
  #   it { should validate_presence_of(:event_id) }
  #   it { should validate_presence_of(:sy_club_id) }
  #   it { should validate_uniqueness_of(:sy_club_member_id).scoped_to(:event_id) }
  # end
end